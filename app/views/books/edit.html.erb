<h1><%= t('views.title.edit', name: Book.model_name.human).capitalize %></h1>

<%= render "form", book: @book %>

<br>

<nav class="page-nav">
  <%= link_to t('views.show', name: Book.model_name.human).capitalize, @book %> |
  <%= link_to t('views.back', name: Book.model_name.human).capitalize, books_path %>
</nav>
