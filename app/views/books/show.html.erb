<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('views.edit', name: Book.model_name.human).capitalize, edit_book_path(@book) %> |
  <%= link_to t('views.back', name: Book.model_name.human).capitalize, books_path %>

  <%= button_to t('views.destroy', name: Book.model_name.human).capitalize, @book, method: :delete %>
</nav>
