<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('book_views.edit_this_book', name: Book.model_name.human), edit_book_path(@book) %> |
  <%= link_to t('book_views.back_to_books', name: Book.model_name.human), books_path %>

  <%= button_to t('book_views.destroy_this_book', name: Book.model_name.human), @book, method: :delete %>
</nav>
